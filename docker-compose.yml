version: '3'

services:
  postgresql:
    container_name: 'ttr_db'
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ticket_to_ride_db
    ports:
      - "5432:5432"